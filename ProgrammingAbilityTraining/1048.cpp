#include <bits/stdc++.h>

using namespace std;

vector<vector<bool>> v()

int main(void) {
    int n;
    cin >> n;
    for (int i = 0; i < n; i++) {
        int x, y;
        cin >> x >> y;

        for ()
    }
}